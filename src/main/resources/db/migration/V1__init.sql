CREATE TABLE microwave_program (
                                   id SERIAL PRIMARY KEY,
                                   name VARCHAR(100) NOT NULL,
                                   power INT NOT NULL,
                                   duration INT NOT NULL
);
